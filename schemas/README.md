# Data Schemas

Schema files for dataspace views, evidence status, source-pack status, publication gates and route manifests.

Initial schemas should be added after route aliases and workspace/dataspace manifests are reviewed.
